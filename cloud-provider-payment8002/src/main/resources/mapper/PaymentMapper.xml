<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.parak.springcloud.mapper.PaymentMapper">

    <insert id="create" parameterType="top.parak.springcloud.entity.Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment(serial) values(#{serial})
    </insert>

    <resultMap id="BaseResultMap" type="top.parak.springcloud.entity.Payment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="serial" property="serial" jdbcType="VARCHAR" />
    </resultMap>

    <select id="getPaymentById" parameterType="java.lang.Long" resultMap="BaseResultMap" >
        select * from payment where id = #{id}
    </select>

    <select id="getAllPayment" resultMap="BaseResultMap">
        select * from payment
    </select>

</mapper>
